import React from 'react';




const BuildControlType = (props) => (
    <div className={props.className}>
         <input className={props.className}  type="button" onClick={this.props.clickedType} value={props.value}/>
    </div>
);

export default BuildControlType;